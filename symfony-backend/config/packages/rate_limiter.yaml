framework:
    rate_limiter:
        password_forgot:
            policy: 'fixed_window'
            limit: 5
            interval: '1 hour'
        password_reset:
            policy: 'fixed_window'
            limit: 10
            interval: '15 minutes'
        contact:
            policy: 'fixed_window'
            limit: 5
            interval: '1 hour'
